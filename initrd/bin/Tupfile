ifeq (@(GITTUP_INITRD),y)
include_rules

include $(GITTUP_ROOT)/busybox/install.tup
include $(GITTUP_ROOT)/ncurses/install.tup
include $(GITTUP_ROOT)/nethack/install.tup
include $(GITTUP_ROOT)/alsa-utils/install.tup
include $(GITTUP_ROOT)/mplayer/install.tup
include $(GITTUP_ROOT)/pciutils/install.tup
include $(GITTUP_ROOT)/sshfs-fuse/install.tup
include $(GITTUP_ROOT)/openssh/install.tup
include $(GITTUP_ROOT)/fuse/install.tup

endif
